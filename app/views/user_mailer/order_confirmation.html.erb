<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for placing your order. Your order id is @order.id></h1>
    <% @order.total = 0 %>
    <% @order.line_items.each do |i| %>
      <% @order_total += (i.product.price * i.quantity) %>
      <p> i.product.name </p>
    <% end %>
      <h2> Your order total is $<%= @order.total $> </h2>
  </body>
</html>